services:
  - type: web
    name: prj301-youtube
    env: docker
    dockerfilePath: Dockerfile
    envVars:
      - key: SERVER_PORT
        value: 8080
      - key: DATABASE_URL
        sync: false
